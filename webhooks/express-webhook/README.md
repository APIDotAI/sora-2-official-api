# Sora 2 Official Express webhook example

Run npm install and npm start, then expose /api/apidot/webhook through a public URL for APIDot callbacks. Production receivers should verify task ownership and handle duplicate callbacks idempotently.

